import { EmptyState } from '@/components/common/EmptyState';

export const Proximamente = () => (
  <EmptyState
    title="Prximamente"
    description="Estamos trabajando en nuevas funcionalidades para este mdulo."
  />
);

export default Proximamente;
